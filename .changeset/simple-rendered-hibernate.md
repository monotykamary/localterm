---
"@monotykamary/localterm-server": minor
---

Hibernate open terminal tabs on graceful daemon shutdown by persisting bounded, rendered normal-buffer text. Exact browser tabs restore that plain history ahead of a fresh shell after a restart, while alternate-screen TUI frames and terminal control state are never written to disk.
