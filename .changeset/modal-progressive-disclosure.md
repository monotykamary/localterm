---
"@monotykamary/localterm": patch
---

Keep modal controls readable with progressive disclosure and bounded dynamic text, including long branch, secret, process, and worktree names. Add a deterministic Pretext-backed width audit so adversarial labels fail the test suite before they reach the UI.
