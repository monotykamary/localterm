---
"@monotykamary/localterm-server": patch
---

Keep reattached browser tabs scoped to their shell's live directory so a cached Git diff summary from the session's original repository cannot flash in a non-repository tab.
