---
"@monotykamary/localterm-server": minor
---

Add a `.worktreeinclude` editor to the worktree settings panel. The file can now be read and written via `/api/git/worktrees/include-file`, with a UI that shows whether it exists and lets users create or update it.
