---
"@monotykamary/localterm-server": patch
---

Unpacked Chrome extension: tab groups, pin/mute/move/discard, window bounds, richer TargetInfo, OOPIF/worker targets via `chrome.debugger.getTargets`, and `Browser.grantPermissions` via contentSettings. Reload the extension after `tabGroups` / `contentSettings`.
