---
"@monotykamary/pi-localterm": patch
---

Hand nested bash execution to compatible pi-fabric through versioned middleware while preserving child-environment secret scrubbing, streaming output redaction, shell settings, and standalone behavior. Background jobs retain filtered live logs, and buffered redaction is flushed safely on errors and cancellation.
