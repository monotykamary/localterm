---
"@monotykamary/pi-localterm": patch
---

Statically import the extension activation so pi's loader resolves the bundled runtime imports (@earendil-works/pi-coding-agent, @earendil-works/pi-tui), fixing `Cannot find package` when pi starts inside LocalTerm.
