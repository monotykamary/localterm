---
"@monotykamary/localterm": minor
"@monotykamary/localterm-server": minor
---

Add first-class Helium browser support to localterm's CDP integration. The daemon now detects Helium's verified user-data directory on macOS, Linux, and Windows, can open `chrome://inspect` in Helium, and reports the browser as Helium instead of relying on generic fallback behavior.
